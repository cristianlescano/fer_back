import express from 'express';
const router = express();

import {funciono} from './functions.js';

router.get('/checkVersion', 
funciono
);
router.get('/ofertas', 
funciono
);

export default router;